{
  "name": "DataSource",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "statcan",
        "bc_health",
        "fnha",
        "manual_upload",
        "api",
        "other"
      ],
      "description": "Type of data source"
    },
    "medium": {
      "type": "string",
      "enum": [
        "table",
        "report",
        "rss_feed",
        "api",
        "dataset",
        "document",
        "map_layer",
        "other"
      ],
      "default": "dataset",
      "description": "The kind of artefact this source represents. Reports are static and not resynced; rss_feed and api are pollable; tables/datasets are structured records."
    },
    "url": {
      "type": "string",
      "description": "API or dataset URL"
    },
    "description": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "enum": [
        "chronic_disease",
        "mental_health",
        "substance_use",
        "maternal_child",
        "social_determinants",
        "demographics",
        "mortality",
        "access_to_care",
        "other"
      ]
    },
    "region": {
      "type": "string",
      "description": "Primary BC region the source covers (used by filters and ingest tagging)"
    },
    "notes": {
      "type": "string",
      "description": "Free-form analyst notes attached to the source"
    },
    "last_synced": {
      "type": "string",
      "format": "date-time"
    },
    "sync_frequency": {
      "type": "string",
      "enum": [
        "manual",
        "daily",
        "weekly",
        "monthly"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "error",
        "pending"
      ],
      "default": "pending"
    },
    "record_count": {
      "type": "number"
    },
    "metadata": {
      "type": "object"
    }
  },
  "required": [
    "name",
    "type"
  ]
}